# Zigistry's Dependents dependency graph

To run this project:
We will first need zigistry's database.

```sh
turso auth login
turso db export zigistry
```

This will download the zigistry database as `zigistry.db`.

Then, we can run this python program by installing important dependencies.
